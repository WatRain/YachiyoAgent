# 安装：
#   & ".\.venv\Scripts\python.exe" -m pip install -r requirements.txt
# 若国内镜像缺包：
#   & ".\.venv\Scripts\python.exe" -m pip install -r requirements.txt `
#       --extra-index-url https://pypi.org/simple

# UI（Flet 1.0：flet / flet-cli / flet-desktop 锁步发布）
flet[all]>=1.0.0,<2

# 注意：API Key 的持久化**不需要额外依赖**。
# core/secrets.py 用 ctypes 直调 Windows 凭据管理器（advapi32），
# 不再用 flet-secure-storage —— 那个插件走 Flet 的 invoke-method IPC，
# 在真机上会抛 RuntimeError 甚至无限等待，实测把首次引导卡死过。

# 统一的模型调用层（支持任意 OpenAI 兼容端点）
litellm>=1.100

# 异步 SQLite（给会话/记忆落库用）
aiosqlite>=0.20

# 配置校验
pydantic>=2.10,<3

# 图片处理：Live2D 的超大贴图（8192×8192）要先缩成能跑的大小再喂给渲染器，
# 见 app/live2d/textures.py。缩过的版本只写进数据目录，不动用户的模型文件。
pillow>=10

# 开发/测试
pytest>=8
pytest-asyncio>=0.24
