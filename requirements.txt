# 安装：
#   & ".\.venv\Scripts\python.exe" -m pip install -r requirements.txt
# 若国内镜像缺包：
#   & ".\.venv\Scripts\python.exe" -m pip install -r requirements.txt `
#       --extra-index-url https://pypi.org/simple

# 界面不用 Python 装 —— 前端是 Electron（见 desktop/）。
# 这个文件只装后端与核心逻辑要用的东西；
# Electron 侧依赖在 desktop/package.json 里（npm install）。

# 注意：API Key 的持久化**不需要额外依赖**。
# core/secrets.py 用 ctypes 直调 Windows 凭据管理器（advapi32），调用在
# 当前进程内完成，既不经过任何插件 IPC，也不用把密钥写进文件。

# 后端：本机 HTTP + WebSocket 服务（界面和接口都从这一个源走）
fastapi>=0.115
uvicorn>=0.30

# 统一的模型调用层（支持任意 OpenAI 兼容端点）
litellm>=1.100

# 异步 SQLite（给会话/记忆落库用）
aiosqlite>=0.20

# 配置校验
pydantic>=2.10,<3

# 图片处理：Live2D 的超大贴图（8192×8192）要先缩成能跑的大小再喂给渲染器，
# 见 app/live2d/textures.py。缩过的版本只写进数据目录，不动用户的模型文件。
pillow>=10

# 开发/测试
pytest>=8
pytest-asyncio>=0.24
