# 虚拟环境
.venv/
venv/
env/

# 机密 —— 最重要的一行
.env
.env.*
!.env.example

# 本地文档：教程、打包清单、agent loop 说明
# 决定不随代码上传（含本地路径与产品思路）。文件仍在本机，随时可读。
docs/

# 用户数据 / 运行时
.devdata/
.flet/
*.db
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.pytest_tmp/
.ptmp/
pytest-cache-files-*/
.mypy_cache/
.ruff_cache/

# 打包产物
dist/
build/
*.spec

# 打包时临时下载的 wheel
wheels/
